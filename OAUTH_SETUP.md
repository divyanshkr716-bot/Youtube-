# Google/YouTube OAuth setup

Package: com.divya.ytautoshorts
Scope: https://www.googleapis.com/auth/youtube.upload

Create an Android OAuth client in Google Cloud with the package name and SHA-1 of the key used to build the APK. Enable YouTube Data API v3. Do not commit secrets.

The current code intentionally does not hardcode a client secret or API key.
